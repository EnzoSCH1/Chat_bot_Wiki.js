├── docker-compose.yml    ✅ (avec extra_hosts GitLab)
├── .env                  ✅ (tokens GitLab + Ollama + Wiki)
├── app.py                ✅ (moteur de recherche + API)
├── Dockerfile            ✅ (Python + Git)
├── requirements.txt      ✅ (flask, requests, dotenv, PyYAML)
└── widget.html           ✅ (interface chatbot)